#!/bin/bash 

curl https://sh.rustup.rs -sSf | sh

cargo_packages=()
cargo_packages+=(cargo-update)
cargo_packages+=(ripgrep powerline-rust)
#if ![[ "$(command -v termux-setup-storage)" ]]; then

cargo install "${cargo_packages[@]}"
	
